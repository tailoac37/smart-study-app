# =====================================================
# LOCAL Development Configuration (MySQL Local)
# =====================================================

# MySQL Local Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/smart_study_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=01112004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Database Initialization
# spring.sql.init.mode=always
# spring.sql.init.schema-locations=classpath:schema.sql
