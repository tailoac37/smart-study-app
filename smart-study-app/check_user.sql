SELECT id, username FROM users WHERE username='student2';
SELECT id, username FROM users WHERE username='sinhvien2'; 

-- User đang dùng là sinhvien2, password 123.
-- Nhưng seed data chỉ có student1..student40. 
-- Có thể user sinhvien2 là user tạo thêm?
-- Kiểm tra user 'sinhvien2' trước.
